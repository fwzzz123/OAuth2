package org.example.authorizationserver.config;

import org.springframework.context.annotation.Configuration;
// 若后续用 Redis，再引入相关依赖和配置
// 现在先保留空类，避免报错
@Configuration
public class RedisConfig {
    // 暂时无内容，后续扩展 Redis 存储时补逻辑
}